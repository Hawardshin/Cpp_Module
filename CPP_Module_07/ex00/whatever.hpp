#pragma once
#include <iostream>
class whatever
{
public:
  whatever();
  explicit whatever();
  whatever(const whatever& obj );
  ~whatever();
  whatever& operator=(const whatever& obj );

private:

};
