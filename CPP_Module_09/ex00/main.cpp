# include "BitcoinExchange.hpp"

int main(int argc, char *argv[]){
  if (argc == 1)
    std::cerr << "Error: could not open file.\n";
  else if (argc != 2)
    std::cerr << "Error: argument must be only one!!\n";
  else{

  }
  return (0);
}
